interest calculator

Idea for the Interest calculator

•	Get the input to calculate the interest for range of date, month and year
•	Input fields are principle amount, interest rate per month, range of time (date, month, year)
•	Create the check box for include starting date and current date (it updates the interest amount)
o	Interest amount per month = principle amount * interest rate %
•	Then use multiplication and division to calculate the final interest rate
•	After calculation the date range and interest rate are modified the interest amount would be dynamically changed
•	Display the current date in the top bar

•	History would be stored as a cache in minimum size (Same like calculator in mobile)
•	Create a simple appearance tool to change the theme and customize (background colour and font colour)
•	To build it as a static website in small size (no backend, no database connections)
•	It would be Simple and Professional 
